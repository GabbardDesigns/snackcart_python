class Transaction:
    def __init__(self):
        self.inventory = []

    def addItem(self, item):
        # sort items on add
        pass

    def removeItem(self, item):
        pass

    def calculateTotal(self, item):

        for item in self.inventory:

            if item.qtydiscount:
                check to see if next item is same, if so, check to see if next item is the same, add items up and
                # count how many of an item there are in the cart, apply qtyprice instead of price
        total +=






